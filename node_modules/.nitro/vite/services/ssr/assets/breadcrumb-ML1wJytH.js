import { Fragment } from "react";
import { Link } from "@tanstack/react-router";
import { jsx, jsxs } from "react/jsx-runtime";
import { ChevronRight } from "lucide-react";
//#region src/components/breadcrumb.tsx
function Breadcrumb({ items }) {
	return /* @__PURE__ */ jsx("nav", {
		"aria-label": "Breadcrumb",
		className: "text-sm text-muted-foreground",
		children: /* @__PURE__ */ jsx("ol", {
			className: "flex flex-wrap items-center gap-1",
			children: items.map((c, i) => {
				const last = i === items.length - 1;
				return /* @__PURE__ */ jsxs(Fragment, { children: [/* @__PURE__ */ jsx("li", { children: c.to && !last ? /* @__PURE__ */ jsx(Link, {
					to: c.to,
					className: "hover:text-primary",
					children: c.label
				}) : /* @__PURE__ */ jsx("span", {
					className: last ? "text-ink font-medium" : "",
					children: c.label
				}) }), !last && /* @__PURE__ */ jsx(ChevronRight, {
					size: 14,
					className: "text-border"
				})] }, i);
			})
		})
	});
}
//#endregion
export { Breadcrumb as t };
